import {View, Text, StatusBar} from 'react-native';
import {BlurView} from '@react-native-community/blur';

import layout from '../constants/layout';

const DecklistsScreen = () => <Text>Hey</Text>;

export default DecklistsScreen;
